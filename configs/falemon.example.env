# Falemon adapter example.
# The adapter is diagnostic by default. It does not kill Falemon processes.

PD_PROVIDER=falemon

# Adjust these for your machine. The old local script used these ports, but
# this project treats them as user configuration rather than universal defaults.
PD_HTTP_PROXY=http://127.0.0.1:10792
PD_SOCKS_PROXY=socks5h://127.0.0.1:10793
PD_HTTP_PORT=10792
PD_SOCKS_PORT=10793

# Optional Falemon app path overrides.
# PD_FALEMON_APP_DIR="$HOME/Library/Application Support/com.falemon.macos10921"
# PD_FALEMON_HTTP_BIN="$PD_FALEMON_APP_DIR/falemonhttp"
# PD_FALEMON_LF_BIN="$PD_FALEMON_APP_DIR/falemonlf"

# Optional safe restart command. Leave unset for diagnostic-only fallback.
# PD_RESTART_COMMAND='launchctl kickstart -k "gui/$(id -u)/com.example.falemon"'
